export interface Contact {
    id: number;
    name: string;
    username: string;
    email: string;
    phone: string;
    website: string;
}

export interface Company {
    name: string;
    catchPhrase: string;
    bs: string;
}

